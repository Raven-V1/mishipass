# MishiPass — Demo Flow

Derived from Constitution Section 21 (LOCKED).

**What the demo must prove:** the same physical QR tag becomes different tools
depending on the cat's situation. The QR code itself never changes — the
TypeScript Worker reads the cat's current mode from D1 and returns the appropriate
interface.

## Sequence

| Step | Action | What it shows |
|---|---|---|
| 1 | Register cat | System generates a public ID (e.g. `MP-MX-7X3B-9K21`) and its QR |
| 2 | Scan QR → Active Profile | Photo, public info, registered-country badge, privacy-controlled contact |
| 3 | Switch to Missing Alert | Last seen, city, area, reward setting, Recovery Board publish |
| 4 | Scan the same QR → Missing Alert | A different experience from the same physical QR |
| 5 | Finder submits a sighting report | Location, photo, message — owner receives it |
| 6 | Generate WhatsApp card | Two versions: with phone number / with relay link only |
| 7 | Switch to Vet Visit | Temporary vet session activated |
| 8 | Scan the same QR → Vet form | Vet adds a vaccine, a medication record, a sticker photo |
| 9 | Save & Finish Visit | Record saves to the cartilla; QR returns to Active Profile |

## Recording notes (for Day 14)

- Keep the video under 5 minutes (submission requirement).
- Narrate in English; the QR-scan moments (steps 2, 4, 8) are the core of the
  demo — show the same tag producing different pages.
- State explicitly on camera that the QR is static and only the mode changes, to
  avoid the "dynamic QR" being read as a changing code.
- Do not show real owner contact details or any private cartilla data on a public
  surface.

> Status: flow is locked; the recorded video and final screen captures are
> produced on Day 14 per Section 19.
